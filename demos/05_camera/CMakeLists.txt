ADD_EXECUTABLE(05_camera WIN32 main.cpp)

TARGET_LINK_LIBRARIES(05_camera core)
target_include_directories(05_camera PRIVATE ../include)

TARGET_COMPILE_DEFINITIONS(05_camera PRIVATE UNICODE _UNICODE)
TARGET_COMPILE_DEFINITIONS(05_camera PRIVATE RESOURCES_PATH="${PROJECT_SOURCE_DIR}/resources")
